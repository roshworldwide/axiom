\* TLC model for AcousticAuth.tla  (Axiom — Phase 3, Week 14).
\* Both adversaries enabled, plus bounded clock skew (MaxSkew = 1). TLC confirms
\* tokens still expire correctly (Freshness) despite skewed verifier clocks.

SPECIFICATION Spec

CONSTANTS
    Tokens = {tk1, tk2}
    Envs = {en1, en2}
    MaxTime = 4
    TTL = 2
    MaxSkew = 1
    Replay = TRUE
    Relay = TRUE

INVARIANT TypeOK
INVARIANT ReplayResistance
INVARIANT RelayResistance
INVARIANT Freshness
