\* TLC model for AcousticAuth.tla  (Axiom — Phase 3, Week 12).
\* The capture + REPLAY adversary is enabled; the cross-environment relay
\* adversary is added in Week 13 (Relay = TRUE).

SPECIFICATION Spec

CONSTANTS
    Tokens = {tk1, tk2}
    Envs = {en1, en2}
    MaxTime = 4
    TTL = 2
    Replay = TRUE
    Relay = FALSE

INVARIANT TypeOK
INVARIANT ReplayResistance
INVARIANT RelayResistance
INVARIANT Freshness
