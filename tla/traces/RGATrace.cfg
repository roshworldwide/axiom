\* Deterministic scenario that pins the RGA trace fixture
\* (visible id sequence + tombstone set).
SPECIFICATION TSpec

CONSTANTS
    Replicas = {1, 2}
    MaxInserts = 2
    Origin = o

INVARIANT TraceMatches
