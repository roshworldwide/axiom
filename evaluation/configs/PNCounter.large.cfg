\* Widened PN-Counter for the large-scale TLC run (evaluation/large_run.sh).
\* 3 replicas, up to 3 ops each, symmetry over replicas. Invariants only.
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2, r3}
    MaxOps = 3

SYMMETRY Symm

INVARIANT TypeOK
INVARIANT NoFabrication
