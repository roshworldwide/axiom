\* Widened OR-Set for the large-scale TLC run (evaluation/large_run.sh).
\* 3 replicas, 2 elements, up to 2 adds per element, symmetry over replicas.
\* Invariants only (the bounded CI model also checks the Monotonic property).
SPECIFICATION Spec

CONSTANTS
    Replicas = {r1, r2, r3}
    Elements = {e1, e2}
    MaxAdds = 2

SYMMETRY Symm

INVARIANT TypeOK
INVARIANT TombstonesObserved
